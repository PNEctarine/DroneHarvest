# 🛰️ Симуляция сбора ресурсов дронами

Небольшой проект на **Unity**, в котором дроны двух фракций соревнуются за сбор ресурсов.  
Реализован с использованием **ECS (LeoEcs Lite)** и **Zenject** для внедрения зависимостей.

---

## 🚀 Что реализовано

- Две фракции: **Серая** и **Красная**, каждая управляет своими дронами.  
- Полный цикл поведения дронов:
  - Взлёт с площадки  
  - Поиск ближайшего свободного ресурса  
  - Полёт к ресурсу  
  - Подъём ресурса с анимацией  
  - Возврат на базу и выгрузка  
- Начисление очков фракциям за доставленные ресурсы  
- Включение/выключение визуализации пути полёта дрона
- Настройка таймера генерации ресурсов
- Настройка количества активных дронов и скорости
- Управление

---

## 🧩 Архитектура проекта

### Компоненты (ECS Components)

| Компонент | Описание |
|-----------|----------|
| `StarshipComponent` | Состояние дрона, таймер, ссылка на NavMeshAgent, переносимый ресурс |
| `ResourceComponent` | Данные ресурса: трансформ, состояние `IsTaken`, ID сущности |
| `LocationComponent` | Данные о площадках запуска и позициях фракций |

### Системы (ECS Systems)

| Система | Описание |
|---------|----------|
| `StarshipsSpawnSystem` | Система управления передвиженими кораблей |
| `StarshipsMoveSystem` | Управление поведением дронов (`Idle → TakeOff → Flying → Collecting → Returning`), таймерами, начисление очков, возврат на базу |
| `ResourcesSpawnSystem` | Система спауна ресурсов |

### MonoBehaviour-классы

- **`StarshipBehaviour`** — визуальное и навигационное поведение дрона (полёт, взлёт, посадка, визуализация пути)  
- **`LaunchingPad`** — площадка старта для дронов  
- **`GameUI`** — пользовательский интерфейс, управление скоростью и очками  

### Данные

- **`SessionData`** — текущие игровые параметры: скорость дронов, количество активных единиц, счёт фракций  
- **`GameEvents`** — централизованная система событий (например, изменение скорости)

### Конфиги

- **`ResourcesConfig`** — конфиг для расширения визуала ресурсов 
- **`StarshipsConfig`** — конфиг для расширения визуала космических кораблей

---

## ⚙️ Использованные технологии

| Инструмент / Библиотека | Назначение |
|--------------------------|------------|
| **Unity 2022.3.62f3** | Игровой движок |
| **LeoEcs Lite** | ECS-фреймворк для логики и производительности |
| **Zenject** | Внедрение зависимостей |
| **DOTween** | Анимации перемещения объектов |
| **NavMesh** | Навигация дронов |
| **LineRenderer** | Отрисовка пути движения |
| **C#** | Язык программирования проекта |

---

## 🧠 Подход к реализации

- Логика разделена по **ECS-принципам**: данные (компоненты), логика (системы), визуализация (MonoBehaviour)  
- Каждый дрон действует **автономно**, в рамках своего состояния  
- **Таймеры** вместо корутин для предсказуемого поведения  
- **Zenject** обеспечивает чистую архитектуру и лёгкую интеграцию данных и событий  
- Код легко расширяем: можно добавлять новые ресурсы, поведения дронов или улучшать ИИ  

---

## 🧾 Управление

- **WASD / ПКМ / края экрана** – перемещение камеры по плоскости  
- **Скролл мыши** – приближение/отдаление камеры  
- **Средняя кнопка мыши** – вращение камеры вокруг центра сцены
